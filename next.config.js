/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['katzchwat.com', 'assets-global.website-files.com', 'images.pexels.com']
    }
}

module.exports = nextConfig
